# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ScoringSystem' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Swifter', '~> 1.5.0'
  pod 'Firebase', '>= 2.5.0'
  pod 'Firebase/Firestore'
  # Pods for ScoringSystem

  target 'ScoringSystemTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ScoringSystemUITests' do
    # Pods for testing
  end

end
